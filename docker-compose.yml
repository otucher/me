version: '3.8'

services:
  traefik:
    image: traefik:latest
    command:
      - --api.insecure=true  # allow dashboard
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --log.level=DEBUG
    ports:
      - 80:80
      - 8080:8080
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  client:
    build: client
    labels:
      - traefik.enable=true
      - traefik.http.routers.client.rule=Host(`client.resume.localhost`)

  server:
    build: server
    volumes:
      - ~/.aws:/root/.aws
    environment:
      - AWS_PROFILE=$AWS_PROFILE
    labels:
      - traefik.enable=true
      - traefik.http.routers.server.rule=Host(`server.resume.localhost`)
      - traefik.http.services.server.loadbalancer.server.port=80
